import { StyleSheet, Text, View } from 'react-native'
import { GREY } from '../../assets/colors'
import { BORDER_WIDTH } from '../../assets/sizes'
export const styles = StyleSheet.create({
    line: {
        height: BORDER_WIDTH,
        backgroundColor: GREY
    }
})