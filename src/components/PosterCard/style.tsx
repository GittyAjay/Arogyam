import { StyleSheet, Text, View } from 'react-native'
import { GREY, BLUE_SHADE, YELLOW, WHITE } from '../../assets/colors'
import { BORDER_RADIUS } from '../../assets/sizes'

export const styles = StyleSheet.create({
    poster: { height: 150, borderRadius: BORDER_RADIUS, backgroundColor: WHITE }
})