import { StyleSheet, Text, View } from 'react-native'
import { WHITE } from '../../assets/colors'
import { FONT_SMALL } from '../../assets/sizes'
export const styles = StyleSheet.create({
    description: {
        fontSize: FONT_SMALL,
        fontFamily: 'Nunito-Regular',
        color: WHITE
    }
})